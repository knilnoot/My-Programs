/**
 * Created by covingtonbr441 on 1/18/2017.
 */import java.awt.*;

/*public class ship {
    public double [] sCoords = {0, 1, 1, -1, 0, 0, -1, -1, 0, 1}, fCoords = {0, 0, .5, -.5, 0, -1.5, -.5, -.5, 0, 0};
    public double x, y, xSpeed, ySpeed, rot, rotVel, rotDec;
    public int [] [] shipPaintArr = new int [4] [5];
    public ship () {
        rotVel = 2;
        rotDec = .8;
        rot = 0;
        for (int i = 0; i < 10; i += 2) {
            shipPaintArr [0] [i] = (int) (Math.sqrt(Math.pow(sCoords [i], 2) + Math.pow(sCoords [i + 1], 2)) * Math.sin(Math.toRadians(rot) + Math.toRadians(Math.atan(sCoords [i] / sCoords [i + 1]))));
            shipPaintArr [0] [i] = (int) (Math.sqrt(Math.pow(sCoords [i], 2) + Math.pow(sCoords [i + 1], 2)) * Math.cos(Math.toRadians(rot) + Math.toRadians(Math.atan(sCoords [i] / sCoords [i + 1]))));
        }
    }
    public void paint (Graphics g) {
        rot++;
        g.drawPolygon(shipPaintArr [0],shipPaintArr [1], 5);
        //this is some text
    }
}*/